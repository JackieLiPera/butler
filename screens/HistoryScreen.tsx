import { Text, View } from "react-native";

export default function HistoryScreen() {
  // query for all requests for a particular user.
  // need a joins table that saves people who accept the request
  return (
    <View>
      <Text>History Screen</Text>
    </View>
  );
}
